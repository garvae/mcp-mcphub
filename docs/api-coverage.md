# API Coverage

Generated from upstream snapshot `samanhappy/mcphub@v1.0.15`.

This matrix classifies every coverageable route from `src/routes/index.ts`.

## Legend

- `Classification`: exposure or handling category used by the management MCP.
- `Profile`: highest exposure profile required to expose the route as an MCP capability.
- `Risk`: operational risk class used for confirmations and redaction policy.

## Classified Routes

| Route                                                                | Authenticated | Classification   | Profile | Risk               | MCP Surface                                       | Notes                                                                                                                     |
| -------------------------------------------------------------------- | ------------- | ---------------- | ------- | ------------------ | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `GET /.well-known/mcp-marketplace`                                   | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_get_marketplace_well_known`         | Public discovery metadata for the local marketplace catalog.                                                              |
| `GET /.well-known/oauth-authorization-server`                        | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `GET /.well-known/oauth-protected-resource`                          | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `GET /api/:name/openapi.json`                                        | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_get_group_downstream_openapi_spec`  | Group-scoped downstream OpenAPI document for MCPHub tool execution.                                                       |
| `GET /api/:name/tools/:serverName/:toolName`                         | no            | `unsupported`    | -       | -                  | none                                              | These routes duplicate MCPHub downstream gateway behavior and are intentionally excluded from the management MCP surface. |
| `POST /api/:name/tools/:serverName/:toolName`                        | no            | `unsupported`    | -       | -                  | none                                              | These routes duplicate MCPHub downstream gateway behavior and are intentionally excluded from the management MCP surface. |
| `GET /api/activities`                                                | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_activities`                    | Lists activity records when MCPHub runs in database mode.                                                                 |
| `GET /api/activities/:id`                                            | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_activity`                       | Returns a single activity record by id.                                                                                   |
| `GET /api/activities/available`                                      | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_check_activities_available`         | Reports whether activity endpoints are available on the current MCPHub instance.                                          |
| `DELETE /api/activities/cleanup`                                     | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_cleanup_activities`                 | Activity cleanup deletes history and therefore remains destructive.                                                       |
| `GET /api/activities/filters`                                        | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_activity_filters`               | Returns available filter values for the activity feed.                                                                    |
| `GET /api/activities/stats`                                          | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_activity_stats`                 | Returns aggregate activity statistics in database mode.                                                                   |
| `POST /api/auth/change-password`                                     | yes           | `internal`       | -       | -                  | none                                              | Authentication mutation routes are used by the REST client in JWT mode but are not exposed as management tools.           |
| `GET /api/auth/keys`                                                 | yes           | `admin`          | `admin` | `dangerous_config` | tool: `mcphub_list_bearer_keys`                   | Bearer key listings are secret-sensitive and remain administrative.                                                       |
| `POST /api/auth/keys`                                                | yes           | `admin`          | `admin` | `dangerous_config` | tool: `mcphub_create_bearer_key`                  | Creating a bearer key changes control-plane access and remains dangerous configuration.                                   |
| `DELETE /api/auth/keys/:id`                                          | yes           | `admin`          | `admin` | `dangerous_config` | tool: `mcphub_delete_bearer_key`                  | Deleting a bearer key is destructive for existing clients and remains dangerous configuration.                            |
| `PUT /api/auth/keys/:id`                                             | yes           | `admin`          | `admin` | `dangerous_config` | tool: `mcphub_update_bearer_key`                  | Updating bearer key properties changes privileged API access.                                                             |
| `POST /api/auth/login`                                               | no            | `internal`       | -       | -                  | none                                              | Authentication mutation routes are used by the REST client in JWT mode but are not exposed as management tools.           |
| `POST /api/auth/register`                                            | no            | `internal`       | -       | -                  | none                                              | Authentication mutation routes are used by the REST client in JWT mode but are not exposed as management tools.           |
| `GET /api/auth/user`                                                 | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_current_user`                   | Returns the current authenticated MCPHub user.                                                                            |
| `GET /api/better-auth/user`                                          | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_better_auth_user`               | Returns the resolved Better Auth user for the current request when Better Auth is enabled.                                |
| `GET /api/changelog/update-info`                                     | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_changelog_update_info`          | Returns MCPHub update metadata proxied from the changelog service.                                                        |
| `GET /api/cloud/categories`                                          | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_cloud_categories`              | Lists cloud market categories.                                                                                            |
| `GET /api/cloud/categories/:category`                                | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_cloud_servers_by_category`      | Lists cloud market servers for a category.                                                                                |
| `GET /api/cloud/servers`                                             | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_cloud_servers`                 | Lists cloud market servers.                                                                                               |
| `GET /api/cloud/servers/:name`                                       | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_cloud_server`                   | Returns a single cloud market entry.                                                                                      |
| `GET /api/cloud/servers/:serverName/tools`                           | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_cloud_server_tools`             | Lists callable tools for a cloud market server.                                                                           |
| `POST /api/cloud/servers/:serverName/tools/:toolName/call`           | yes           | `all`            | `all`   | `dangerous_config` | tool: `mcphub_call_cloud_tool`                    | Calling a cloud market tool is intentionally reserved for the all profile because it crosses into downstream execution.   |
| `GET /api/cloud/servers/search`                                      | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_search_cloud_servers`               | Searches cloud market servers by query string.                                                                            |
| `GET /api/cloud/tags`                                                | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_cloud_tags`                    | Lists cloud market tags.                                                                                                  |
| `GET /api/cloud/tags/:tag`                                           | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_cloud_servers_by_tag`           | Lists cloud market servers for a tag.                                                                                     |
| `GET /api/cost/groups`                                               | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_group_costs`                    | Returns context footprint cost data for groups.                                                                           |
| `GET /api/cost/servers`                                              | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_server_costs`                   | Returns context footprint cost data for servers.                                                                          |
| `GET /api/groups`                                                    | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_groups`                        | Lists MCPHub groups and their high-level metadata.                                                                        |
| `POST /api/groups`                                                   | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_create_group`                       | Creates a new group and changes routing behavior for downstream tools.                                                    |
| `DELETE /api/groups/:id`                                             | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_delete_group`                       | Deleting a group is destructive and must require explicit confirmation.                                                   |
| `GET /api/groups/:id`                                                | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_group`                          | Returns a single group definition and summary metadata.                                                                   |
| `PUT /api/groups/:id`                                                | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_update_group`                       | Updates group metadata and membership behavior.                                                                           |
| `GET /api/groups/:id/server-configs`                                 | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_group_server_configs`           | Returns all per-server group overrides for a group.                                                                       |
| `GET /api/groups/:id/server-configs/:serverName`                     | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_group_server_config`            | Returns a single per-server group configuration override.                                                                 |
| `PUT /api/groups/:id/server-configs/:serverName/tools`               | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_update_group_server_tools`          | Updates group-scoped tool selection for a server.                                                                         |
| `GET /api/groups/:id/servers`                                        | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_group_servers`                 | Lists server membership for a single group.                                                                               |
| `POST /api/groups/:id/servers`                                       | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_add_server_to_group`                | Adds an existing server to a group without changing the server definition itself.                                         |
| `DELETE /api/groups/:id/servers/:serverName`                         | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_remove_server_from_group`           | Removes a server from a group while preserving the server definition.                                                     |
| `PUT /api/groups/:id/servers/batch`                                  | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_replace_group_servers`              | Batch replacement of group membership is operational but broad enough to require careful audit context.                   |
| `POST /api/groups/batch`                                             | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_batch_create_groups`                | Batch group creation is a broad infrastructure mutation and stays destructive.                                            |
| `DELETE /api/logs`                                                   | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_clear_logs`                         | Log clearing is destructive because it deletes audit evidence.                                                            |
| `GET /api/logs`                                                      | yes           | `safe`           | `safe`  | `secret_sensitive` | tool: `mcphub_get_logs`                           | Returns MCPHub logs and must redact tokens, headers, and environment values.                                              |
| `GET /api/logs/stream`                                               | yes           | `streaming-only` | -       | -                  | none                                              | Log streaming should be modeled as a future MCP resource subscription, not a request/response tool.                       |
| `GET /api/market/categories`                                         | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_market_categories`             | Lists market categories.                                                                                                  |
| `GET /api/market/categories/:category`                               | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_market_servers_by_category`     | Lists market servers for a category.                                                                                      |
| `GET /api/market/servers`                                            | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_market_servers`                | Lists local market catalog servers.                                                                                       |
| `GET /api/market/servers/:name`                                      | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_market_server`                  | Returns a single market catalog entry.                                                                                    |
| `GET /api/market/servers/search`                                     | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_search_market_servers`              | Searches market catalog servers by query string.                                                                          |
| `GET /api/market/tags`                                               | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_market_tags`                   | Lists market tags.                                                                                                        |
| `GET /api/market/tags/:tag`                                          | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_market_servers_by_tag`          | Lists market servers for a tag.                                                                                           |
| `GET /api/mcp-settings/export`                                       | yes           | `safe`           | `safe`  | `secret_sensitive` | tool: `mcphub_export_settings`                    | Settings export remains secret-sensitive and should only expose redacted content unless explicitly overridden.            |
| `POST /api/mcp/:serverName/prompts/:promptName`                      | yes           | `unsupported`    | -       | -                  | none                                              | These routes duplicate MCPHub downstream gateway behavior and are intentionally excluded from the management MCP surface. |
| `POST /api/mcpb/upload`                                              | yes           | `all`            | `all`   | `dangerous_config` | tool: `mcphub_upload_mcpb_bundle`                 | MCPB upload accepts multipart content and remains dangerous configuration.                                                |
| `GET /api/oauth/clients`                                             | yes           | `admin`          | `admin` | `dangerous_config` | tool: `mcphub_list_oauth_clients`                 | OAuth client management is administrative and secret-sensitive.                                                           |
| `POST /api/oauth/clients`                                            | yes           | `admin`          | `admin` | `dangerous_config` | tool: `mcphub_create_oauth_client`                | Creating an OAuth client introduces new credentials and remains dangerous configuration.                                  |
| `DELETE /api/oauth/clients/:clientId`                                | yes           | `admin`          | `admin` | `dangerous_config` | tool: `mcphub_delete_oauth_client`                | Deleting an OAuth client is destructive and changes auth posture.                                                         |
| `GET /api/oauth/clients/:clientId`                                   | yes           | `admin`          | `admin` | `dangerous_config` | tool: `mcphub_get_oauth_client`                   | OAuth client inspection remains administrative because secrets and redirect URIs require careful redaction.               |
| `PUT /api/oauth/clients/:clientId`                                   | yes           | `admin`          | `admin` | `dangerous_config` | tool: `mcphub_update_oauth_client`                | Updating OAuth client configuration can break auth flows and is classified as dangerous configuration.                    |
| `POST /api/oauth/clients/:clientId/regenerate-secret`                | yes           | `all`            | `all`   | `dangerous_config` | tool: `mcphub_regenerate_oauth_client_secret`     | Secret regeneration is highly sensitive because it invalidates existing integrations immediately.                         |
| `GET /api/openapi.json`                                              | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_get_downstream_openapi_spec`        | This OpenAPI document describes downstream MCP tool execution, not the management API itself.                             |
| `GET /api/openapi/servers`                                           | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_list_openapi_servers`               | Lists downstream servers visible to the OpenAPI generator.                                                                |
| `GET /api/openapi/stats`                                             | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_get_openapi_stats`                  | OpenAPI generator statistics for downstream tool exposure.                                                                |
| `GET /api/prompts`                                                   | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_builtin_prompts`               | Lists built-in MCPHub prompts.                                                                                            |
| `POST /api/prompts`                                                  | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_create_builtin_prompt`              | Creates a built-in prompt exposed by MCPHub itself.                                                                       |
| `DELETE /api/prompts/:id`                                            | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_delete_builtin_prompt`              | Deletes a built-in prompt and therefore requires destructive confirmation.                                                |
| `GET /api/prompts/:id`                                               | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_builtin_prompt`                 | Returns a built-in prompt definition by id.                                                                               |
| `PUT /api/prompts/:id`                                               | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_update_builtin_prompt`              | Updating a built-in prompt changes model-facing instructions and remains an operational write.                            |
| `GET /api/registry/servers`                                          | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_registry_servers`              | Lists MCP registry entries proxied by MCPHub.                                                                             |
| `GET /api/registry/servers/version`                                  | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_registry_server_version`        | Returns a specific registry server version payload.                                                                       |
| `GET /api/registry/servers/versions`                                 | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_registry_server_versions`       | Returns available versions for a registry server entry.                                                                   |
| `GET /api/resources`                                                 | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_builtin_resources`             | Lists built-in MCPHub resources.                                                                                          |
| `POST /api/resources`                                                | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_create_builtin_resource`            | Creates a built-in resource exposed by MCPHub itself.                                                                     |
| `DELETE /api/resources/:id`                                          | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_delete_builtin_resource`            | Deletes a built-in resource and therefore requires destructive confirmation.                                              |
| `GET /api/resources/:id`                                             | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_builtin_resource`               | Returns a built-in resource definition by id.                                                                             |
| `PUT /api/resources/:id`                                             | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_update_builtin_resource`            | Updating a built-in resource changes model-visible context and remains an operational write.                              |
| `POST /api/resources/read`                                           | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_read_builtin_resource`              | Reads built-in resource content and should stay read-only in the MCP surface.                                             |
| `GET /api/servers`                                                   | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_list_servers`                       | Lists MCPHub server definitions with secret-bearing fields redacted in tool output.                                       |
| `POST /api/servers`                                                  | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_create_server`                      | Creates a server definition; stdio command-based servers remain gated by policy flags.                                    |
| `DELETE /api/servers/:name`                                          | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_delete_server`                      | Deleting a server is destructive and must require explicit confirmation fields.                                           |
| `GET /api/servers/:name`                                             | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_server`                         | Returns a single server configuration with secret-bearing fields redacted.                                                |
| `PUT /api/servers/:name`                                             | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_update_server`                      | Updates server configuration and requires destructive-style confirmation because a bad update can disable service access. |
| `POST /api/servers/:name/reload`                                     | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_reload_server`                      | Reload is operational and reversible but still affects active sessions.                                                   |
| `POST /api/servers/:name/toggle`                                     | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_toggle_server`                      | Enable or disable a managed server without deleting it; dry-run support should be preserved in the MCP layer.             |
| `DELETE /api/servers/:serverName/prompts/:promptName/description`    | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_reset_server_prompt_description`    | Resets server prompt description overrides.                                                                               |
| `PUT /api/servers/:serverName/prompts/:promptName/description`       | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_update_server_prompt_description`   | Prompt description overrides influence model-facing guidance and remain operational writes.                               |
| `POST /api/servers/:serverName/prompts/:promptName/toggle`           | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_toggle_server_prompt`               | Toggles a downstream prompt inside a managed server definition.                                                           |
| `DELETE /api/servers/:serverName/resources/:resourceUri/description` | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_reset_server_resource_description`  | Resets server resource description overrides.                                                                             |
| `PUT /api/servers/:serverName/resources/:resourceUri/description`    | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_update_server_resource_description` | Resource description overrides affect model context and stay in the ops profile.                                          |
| `POST /api/servers/:serverName/resources/:resourceUri/toggle`        | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_toggle_server_resource`             | Toggles a downstream resource inside a managed server definition.                                                         |
| `DELETE /api/servers/:serverName/tools/:toolName/description`        | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_reset_server_tool_description`      | Resets server tool description overrides to upstream defaults.                                                            |
| `PUT /api/servers/:serverName/tools/:toolName/description`           | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_update_server_tool_description`     | Description overrides affect agent behavior and therefore stay in the ops profile with audit context.                     |
| `POST /api/servers/:serverName/tools/:toolName/toggle`               | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_toggle_server_tool`                 | Toggles a downstream tool inside a managed server definition.                                                             |
| `POST /api/servers/batch`                                            | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_batch_create_servers`               | Batch server creation remains destructive because it mutates managed infrastructure at scale.                             |
| `GET /api/settings`                                                  | yes           | `safe`           | `safe`  | `secret_sensitive` | tool: `mcphub_get_settings_snapshot`              | Returns a settings snapshot with secrets redacted by default in the MCP output layer.                                     |
| `PUT /api/system-config`                                             | yes           | `all`            | `all`   | `dangerous_config` | tool: `mcphub_update_system_config`               | System config writes can lock users out or weaken auth posture and therefore remain all-profile dangerous operations.     |
| `POST /api/templates/export`                                         | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_export_config_template`             | Exports a configuration template for operational reuse.                                                                   |
| `GET /api/templates/export/groups/:id`                               | yes           | `ops`            | `ops`   | `safe_write`       | tool: `mcphub_export_group_template`              | Exports a group as a reusable configuration template.                                                                     |
| `POST /api/templates/import`                                         | yes           | `all`            | `all`   | `dangerous_config` | tool: `mcphub_import_config_template`             | Template import can introduce arbitrary server definitions and remains dangerous configuration.                           |
| `GET /api/tools/:serverName/:toolName`                               | no            | `unsupported`    | -       | -                  | none                                              | These routes duplicate MCPHub downstream gateway behavior and are intentionally excluded from the management MCP surface. |
| `POST /api/tools/:serverName/:toolName`                              | no            | `unsupported`    | -       | -                  | none                                              | These routes duplicate MCPHub downstream gateway behavior and are intentionally excluded from the management MCP surface. |
| `POST /api/tools/call/:server`                                       | yes           | `unsupported`    | -       | -                  | none                                              | These routes duplicate MCPHub downstream gateway behavior and are intentionally excluded from the management MCP surface. |
| `GET /api/users`                                                     | yes           | `admin`          | `admin` | `read`             | tool: `mcphub_list_users`                         | User listings are administrative and should stay behind trusted profiles.                                                 |
| `POST /api/users`                                                    | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_create_user`                        | Creating a user mutates access control and remains destructive.                                                           |
| `GET /api/users-stats`                                               | yes           | `safe`           | `safe`  | `read`             | tool: `mcphub_get_user_stats`                     | User statistics are read-only and useful for diagnostics, even though upstream still requires authenticated access.       |
| `DELETE /api/users/:username`                                        | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_delete_user`                        | Deleting a user is destructive and must require explicit confirmation.                                                    |
| `GET /api/users/:username`                                           | yes           | `admin`          | `admin` | `read`             | tool: `mcphub_get_user`                           | User record inspection remains administrative because it exposes identity and role data.                                  |
| `PUT /api/users/:username`                                           | yes           | `admin`          | `admin` | `destructive`      | tool: `mcphub_update_user`                        | Updating a user changes access control and remains destructive.                                                           |
| `GET /config`                                                        | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_get_runtime_config`                 | Public runtime bootstrap configuration for the MCPHub frontend.                                                           |
| `GET /discovery/categories`                                          | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_list_discovery_categories`          | Public discovery categories exposed by MCPHub.                                                                            |
| `GET /discovery/servers`                                             | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_list_discovery_servers`             | Public discovery catalog listing for external installers and MCP clients.                                                 |
| `GET /discovery/servers/:name`                                       | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_get_discovery_server`               | Public discovery details for a single catalog server.                                                                     |
| `GET /discovery/servers/:name/install`                               | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_get_discovery_server_install`       | Public install payload for a discovery server entry.                                                                      |
| `GET /discovery/tags`                                                | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_list_discovery_tags`                | Public discovery tags exposed by MCPHub.                                                                                  |
| `GET /health`                                                        | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_health_check`                       | Basic health endpoint for liveness and quick diagnostics.                                                                 |
| `POST /internal/v1/events`                                           | no            | `internal`       | -       | -                  | none                                              | Hosted internal event ingress is not suitable for MCP exposure.                                                           |
| `GET /internal/v1/hosted/runtime-catalog`                            | no            | `internal`       | -       | -                  | none                                              | Hosted internal runtime catalog is HMAC-protected infrastructure plumbing.                                                |
| `GET /oauth/authorize`                                               | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `POST /oauth/authorize`                                              | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `GET /oauth/callback`                                                | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `POST /oauth/register`                                               | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `DELETE /oauth/register/:clientId`                                   | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `GET /oauth/register/:clientId`                                      | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `PUT /oauth/register/:clientId`                                      | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `POST /oauth/token`                                                  | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `GET /oauth/userinfo`                                                | no            | `internal`       | -       | -                  | none                                              | OAuth authorization-server and dynamic registration routes are intentionally kept out of the management MCP tool surface. |
| `GET /public-config`                                                 | no            | `safe`           | `safe`  | `read`             | tool: `mcphub_get_public_config`                  | Public configuration snapshot; useful for diagnosing skip-auth and similar public flags.                                  |

## Dynamic Routes Excluded From Coverage

These routes are runtime-dynamic and intentionally excluded from the strict coverage gate:

| Route                                | Raw Path              | Handler         | Reason                                                                                  |
| ------------------------------------ | --------------------- | --------------- | --------------------------------------------------------------------------------------- |
| `ALL /<betterAuthConfig.basePath>`   | `${betterAuthPath}`   | `toNodeHandler` | Better Auth mounts depend on runtime-configured base paths and are treated as internal. |
| `ALL /<betterAuthConfig.basePath>/*` | `${betterAuthPath}/*` | `toNodeHandler` | Better Auth mounts depend on runtime-configured base paths and are treated as internal. |
